// const { Notas } = require('../../db');

// const getAllNotasForCarpetas = async (carpetaId) => {
//   try {
//     const notas = await Notas.findAll({ where: { CarpetaId: carpetaId } });
//     return notas;
//   } catch (error) {
//     throw new Error(error.message);
//   }
// };
// module.exports = getAllNotasForCarpetas;
