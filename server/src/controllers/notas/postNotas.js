// const { Notas } = require('../../db');

// const createNotaForCarpetas = async (carpetaId, comment) => {
//   try {
//     const nuevaNota = await Notas.create({ comment, CarpetaId: carpetaId });
//     return nuevaNota;
//   } catch (error) {
//     throw new Error(error.message);
//   }
// };

// module.exports = {
//   createNotaForCarpetas,
// };
